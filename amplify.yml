version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build required for static files"
    build:
      commands:
        - echo "No build required for static files"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
