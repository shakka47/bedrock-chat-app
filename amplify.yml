version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build required for static files"
    build:
      commands:
        - echo "No build required for static files"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []

backend:
  phases:
    build:
      commands:
        - echo "No backend build required for static files"
  artifacts:
    baseDirectory: /tmp
    files:
      - '**/*'
